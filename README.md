# openscap-workbench-windows-build
Provides scripts to build https://github.com/OpenSCAP/scap-workbench for windows in docker

Usage:
=====
```
$ mkdir build
# ./docker/compile.sh
```
Windows files will be in *./build*
